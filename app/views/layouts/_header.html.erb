<nav class="navbar navbar-default navbar-fixed-top">
    <div class="collapse navbar-collapse header1" id="myNavbar">
      <ul class="nav navbar-nav navbar-right">
        <li><%= link_to t(:Viewed), view_index_path, id: "word-li" %></li>
        <li><%= link_to t(:cart), sessions_path,id: "word-li" %></li>
        <li><%= link_to t(:Pay), about_path, id: "word-li" %></li>
        <% if user_signed_in? %>
          <li class="dropdown">
            <button class="btn btn-default dropdown-toggle" type="button"
              id="menu1" data-toggle="dropdown"><%= t :account_title %>
              <span class="caret"></span>
            </button>
            <ul class="dropdown-menu">
              <li>
                <%= link_to t(:setting_title), edit_user_registration_path,
                  id: "word-li" %>
              </li>
              <li class="divider"></li>
              <li><%= link_to t(:view_layout_header_logout),
                destroy_user_session_path, method: :delete, id: "word-li" %>
              </li>
            </ul>
          </li>
        <% else %>
          <li class="dropdown">
             <button class="btn btn-default dropdown-toggle"
              type="button" id="menu1" data-toggle="dropdown">
                <p ><%= t :Login %></p>
            </button>
            <ul class="dropdown-menu">
              <li>
                <%= link_to t(:view_layout_header_login),
                  new_user_session_path %>
              </li>
              <li class="divider"></li>
                <%= link_to t(:view_layout_header_facebook),
                  user_facebook_omniauth_authorize_path,id: "word-li" %>
            </ul>
          </li>
        <% end %>
      </ul>
    </div>
</nav>
